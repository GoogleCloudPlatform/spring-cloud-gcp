/*
 * Copyright 2022 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

package com.google.cloud.retail.v2.spring;

import com.google.api.core.BetaApi;
import com.google.api.gax.core.CredentialsProvider;
import com.google.api.gax.core.ExecutorProvider;
import com.google.api.gax.httpjson.InstantiatingHttpJsonChannelProvider;
import com.google.api.gax.retrying.RetrySettings;
import com.google.api.gax.rpc.HeaderProvider;
import com.google.api.gax.rpc.TransportChannelProvider;
import com.google.cloud.retail.v2.PredictionServiceClient;
import com.google.cloud.retail.v2.PredictionServiceSettings;
import com.google.cloud.spring.core.Credentials;
import com.google.cloud.spring.core.DefaultCredentialsProvider;
import com.google.cloud.spring.global.GlobalProperties;
import java.io.IOException;
import java.util.Collections;
import javax.annotation.Generated;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.boot.autoconfigure.AutoConfiguration;
import org.springframework.boot.autoconfigure.condition.ConditionalOnClass;
import org.springframework.boot.autoconfigure.condition.ConditionalOnMissingBean;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.context.annotation.Bean;
import org.threeten.bp.Duration;

// AUTO-GENERATED DOCUMENTATION AND CLASS.
/**
 * Auto-configuration for {@link PredictionServiceClient}.
 *
 * <p>Provides auto-configuration for Spring Boot
 *
 * <p>The default instance has everything set to sensible defaults:
 *
 * <ul>
 *   <li>The default transport provider is used.
 *   <li>Credentials are acquired automatically through Application Default Credentials.
 *   <li>Retries are configured for idempotent methods but not for non-idempotent methods.
 * </ul>
 */
@Generated("by google-cloud-spring-generator")
@BetaApi("Autogenerated Spring autoconfiguration is not yet stable")
@AutoConfiguration
@ConditionalOnClass(PredictionServiceClient.class)
@ConditionalOnProperty(
    value = "com.google.cloud.retail.v2.spring.auto.prediction-service.enabled",
    matchIfMissing = true)
@EnableConfigurationProperties({PredictionServiceSpringProperties.class, GlobalProperties.class})
public class PredictionServiceSpringAutoConfiguration {
  private final PredictionServiceSpringProperties clientProperties;
  private final GlobalProperties globalProperties;
  private static final Log LOGGER =
      LogFactory.getLog(PredictionServiceSpringAutoConfiguration.class);

  protected PredictionServiceSpringAutoConfiguration(
      PredictionServiceSpringProperties clientProperties, GlobalProperties globalProperties) {
    this.clientProperties = clientProperties;
    this.globalProperties = globalProperties;
  }

  /**
   * Obtains the default credentials provider. The used key will be obtained from Spring Boot
   * configuration data files.
   */
  @Bean
  @ConditionalOnMissingBean
  public CredentialsProvider predictionServiceCredentials() throws IOException {
    if (this.clientProperties.getCredentials().hasKey()) {
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using credentials from PredictionService-specific configuration");
      }
      return ((CredentialsProvider) new DefaultCredentialsProvider(this.clientProperties));
    }
    if (LOGGER.isTraceEnabled()) {
      LOGGER.trace("Using credentials from global configuration");
    }
    return ((CredentialsProvider) new DefaultCredentialsProvider(this.globalProperties));
  }

  /**
   * Returns the default channel provider. The default is gRPC and will default to it unless the
   * useRest option is provided to use HTTP transport instead
   */
  @Bean
  @ConditionalOnMissingBean
  public TransportChannelProvider defaultPredictionServiceTransportChannelProvider() {
    return PredictionServiceSettings.defaultTransportChannelProvider();
  }

  /**
   * Provides a PredictionServiceClient bean configured to use the default credentials provider
   * (obtained with predictionServiceCredentials()) and its default transport channel provider
   * (defaultPredictionServiceTransportChannelProvider()). It also configures the quota project ID
   * if provided. It will configure an executor provider in case there is more than one thread
   * configured in the client
   *
   * <p>Individual retry settings are configured as well. It will use the relevant client library's
   * default retry settings when they are not specified in PredictionServiceSpringProperties.
   */
  @Bean
  @ConditionalOnMissingBean
  public PredictionServiceClient predictionServiceClient(
      @Qualifier("predictionServiceCredentials") CredentialsProvider credentialsProvider,
      @Qualifier("defaultPredictionServiceTransportChannelProvider")
          TransportChannelProvider defaultTransportChannelProvider)
      throws IOException {
    PredictionServiceSettings.Builder clientSettingsBuilder =
        PredictionServiceSettings.newBuilder()
            .setCredentialsProvider(credentialsProvider)
            .setTransportChannelProvider(defaultTransportChannelProvider)
            .setHeaderProvider(this.userAgentHeaderProvider());
    if (this.clientProperties.getQuotaProjectId() != null) {
      clientSettingsBuilder.setQuotaProjectId(this.clientProperties.getQuotaProjectId());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Quota project id set to "
                + this.clientProperties.getQuotaProjectId()
                + ", this overrides project id from credentials.");
      }
    }
    if (this.clientProperties.getExecutorThreadCount() != null) {
      ExecutorProvider executorProvider =
          PredictionServiceSettings.defaultExecutorProviderBuilder()
              .setExecutorThreadCount(this.clientProperties.getExecutorThreadCount())
              .build();
      clientSettingsBuilder.setBackgroundExecutorProvider(executorProvider);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Background executor thread count is "
                + this.clientProperties.getExecutorThreadCount());
      }
    }
    if (this.clientProperties.getUseRest()) {
      clientSettingsBuilder.setTransportChannelProvider(
          PredictionServiceSettings.defaultHttpJsonTransportProviderBuilder().build());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using HTTP transport channel");
      }
    }
    RetrySettings.Builder predictRetrySettingBuilder =
        clientSettingsBuilder.predictSettings().getRetrySettings().toBuilder();
    if (this.clientProperties.getPredictInitialRetryDelay() != null) {
      predictRetrySettingBuilder.setInitialRetryDelay(
          this.clientProperties.getPredictInitialRetryDelay());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "PredictInitialRetryDelay set to "
                + this.clientProperties.getPredictInitialRetryDelay());
      }
    }
    if (this.clientProperties.getPredictRetryDelayMultiplier() != null) {
      predictRetrySettingBuilder.setRetryDelayMultiplier(
          this.clientProperties.getPredictRetryDelayMultiplier());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "PredictRetryDelayMultiplier set to "
                + this.clientProperties.getPredictRetryDelayMultiplier());
      }
    }
    if (this.clientProperties.getPredictMaxRetryDelay() != null) {
      predictRetrySettingBuilder.setMaxRetryDelay(this.clientProperties.getPredictMaxRetryDelay());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "PredictMaxRetryDelay set to " + this.clientProperties.getPredictMaxRetryDelay());
      }
    }
    if (this.clientProperties.getPredictInitialRpcTimeout() != null) {
      predictRetrySettingBuilder.setInitialRpcTimeout(
          this.clientProperties.getPredictInitialRpcTimeout());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "PredictInitialRpcTimeout set to "
                + this.clientProperties.getPredictInitialRpcTimeout());
      }
    }
    if (this.clientProperties.getPredictRpcTimeoutMultiplier() != null) {
      predictRetrySettingBuilder.setRpcTimeoutMultiplier(
          this.clientProperties.getPredictRpcTimeoutMultiplier());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "PredictRpcTimeoutMultiplier set to "
                + this.clientProperties.getPredictRpcTimeoutMultiplier());
      }
    }
    if (this.clientProperties.getPredictMaxRpcTimeout() != null) {
      predictRetrySettingBuilder.setMaxRpcTimeout(this.clientProperties.getPredictMaxRpcTimeout());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "PredictMaxRpcTimeout set to " + this.clientProperties.getPredictMaxRpcTimeout());
      }
    }
    if (this.clientProperties.getPredictTotalTimeout() != null) {
      predictRetrySettingBuilder.setTotalTimeout(this.clientProperties.getPredictTotalTimeout());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "PredictTotalTimeout set to " + this.clientProperties.getPredictTotalTimeout());
      }
    }
    clientSettingsBuilder.predictSettings().setRetrySettings(predictRetrySettingBuilder.build());
    return PredictionServiceClient.create(clientSettingsBuilder.build());
  }

  private HeaderProvider userAgentHeaderProvider() {
    String springLibrary = "spring-autogen-prediction-service";
    String version = this.getClass().getPackage().getImplementationVersion();
    return () -> Collections.singletonMap("user-agent", springLibrary + "/" + version);
  }
}
