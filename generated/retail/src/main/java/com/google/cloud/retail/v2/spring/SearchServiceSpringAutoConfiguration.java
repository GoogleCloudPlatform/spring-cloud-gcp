/*
 * Copyright 2022 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

package com.google.cloud.retail.v2.spring;

import com.google.api.core.BetaApi;
import com.google.api.gax.core.CredentialsProvider;
import com.google.api.gax.core.ExecutorProvider;
import com.google.api.gax.httpjson.InstantiatingHttpJsonChannelProvider;
import com.google.api.gax.retrying.RetrySettings;
import com.google.api.gax.rpc.HeaderProvider;
import com.google.api.gax.rpc.TransportChannelProvider;
import com.google.cloud.retail.v2.SearchServiceClient;
import com.google.cloud.retail.v2.SearchServiceSettings;
import com.google.cloud.spring.core.Credentials;
import com.google.cloud.spring.core.DefaultCredentialsProvider;
import com.google.cloud.spring.global.GlobalProperties;
import java.io.IOException;
import java.util.Collections;
import javax.annotation.Generated;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.boot.autoconfigure.AutoConfiguration;
import org.springframework.boot.autoconfigure.condition.ConditionalOnClass;
import org.springframework.boot.autoconfigure.condition.ConditionalOnMissingBean;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.context.annotation.Bean;
import org.threeten.bp.Duration;

// AUTO-GENERATED DOCUMENTATION AND CLASS.
/**
 * Auto-configuration for {@link SearchServiceClient}.
 *
 * <p>Provides auto-configuration for Spring Boot
 *
 * <p>The default instance has everything set to sensible defaults:
 *
 * <ul>
 *   <li>The default transport provider is used.
 *   <li>Credentials are acquired automatically through Application Default Credentials.
 *   <li>Retries are configured for idempotent methods but not for non-idempotent methods.
 * </ul>
 */
@Generated("by google-cloud-spring-generator")
@BetaApi("Autogenerated Spring autoconfiguration is not yet stable")
@AutoConfiguration
@ConditionalOnClass(SearchServiceClient.class)
@ConditionalOnProperty(
    value = "com.google.cloud.retail.v2.spring.auto.search-service.enabled",
    matchIfMissing = true)
@EnableConfigurationProperties({SearchServiceSpringProperties.class, GlobalProperties.class})
public class SearchServiceSpringAutoConfiguration {
  private final SearchServiceSpringProperties clientProperties;
  private final GlobalProperties globalProperties;
  private static final Log LOGGER = LogFactory.getLog(SearchServiceSpringAutoConfiguration.class);

  protected SearchServiceSpringAutoConfiguration(
      SearchServiceSpringProperties clientProperties, GlobalProperties globalProperties) {
    this.clientProperties = clientProperties;
    this.globalProperties = globalProperties;
  }

  /**
   * Obtains the default credentials provider. The used key will be obtained from Spring Boot
   * configuration data files.
   */
  @Bean
  @ConditionalOnMissingBean
  public CredentialsProvider searchServiceCredentials() throws IOException {
    if (this.clientProperties.getCredentials().hasKey()) {
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using credentials from SearchService-specific configuration");
      }
      return ((CredentialsProvider) new DefaultCredentialsProvider(this.clientProperties));
    }
    if (LOGGER.isTraceEnabled()) {
      LOGGER.trace("Using credentials from global configuration");
    }
    return ((CredentialsProvider) new DefaultCredentialsProvider(this.globalProperties));
  }

  /**
   * Returns the default channel provider. The default is gRPC and will default to it unless the
   * useRest option is provided to use HTTP transport instead
   */
  @Bean
  @ConditionalOnMissingBean
  public TransportChannelProvider defaultSearchServiceTransportChannelProvider() {
    return SearchServiceSettings.defaultTransportChannelProvider();
  }

  /**
   * Provides a SearchServiceClient bean configured to use the default credentials provider
   * (obtained with searchServiceCredentials()) and its default transport channel provider
   * (defaultSearchServiceTransportChannelProvider()). It also configures the quota project ID if
   * provided. It will configure an executor provider in case there is more than one thread
   * configured in the client
   *
   * <p>Individual retry settings are configured as well. It will use the relevant client library's
   * default retry settings when they are not specified in SearchServiceSpringProperties.
   */
  @Bean
  @ConditionalOnMissingBean
  public SearchServiceClient searchServiceClient(
      @Qualifier("searchServiceCredentials") CredentialsProvider credentialsProvider,
      @Qualifier("defaultSearchServiceTransportChannelProvider")
          TransportChannelProvider defaultTransportChannelProvider)
      throws IOException {
    SearchServiceSettings.Builder clientSettingsBuilder =
        SearchServiceSettings.newBuilder()
            .setCredentialsProvider(credentialsProvider)
            .setTransportChannelProvider(defaultTransportChannelProvider)
            .setHeaderProvider(this.userAgentHeaderProvider());
    if (this.clientProperties.getQuotaProjectId() != null) {
      clientSettingsBuilder.setQuotaProjectId(this.clientProperties.getQuotaProjectId());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Quota project id set to "
                + this.clientProperties.getQuotaProjectId()
                + ", this overrides project id from credentials.");
      }
    }
    if (this.clientProperties.getExecutorThreadCount() != null) {
      ExecutorProvider executorProvider =
          SearchServiceSettings.defaultExecutorProviderBuilder()
              .setExecutorThreadCount(this.clientProperties.getExecutorThreadCount())
              .build();
      clientSettingsBuilder.setBackgroundExecutorProvider(executorProvider);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Background executor thread count is "
                + this.clientProperties.getExecutorThreadCount());
      }
    }
    if (this.clientProperties.getUseRest()) {
      clientSettingsBuilder.setTransportChannelProvider(
          SearchServiceSettings.defaultHttpJsonTransportProviderBuilder().build());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using HTTP transport channel");
      }
    }
    RetrySettings.Builder searchRetrySettingBuilder =
        clientSettingsBuilder.searchSettings().getRetrySettings().toBuilder();
    if (this.clientProperties.getSearchInitialRetryDelay() != null) {
      searchRetrySettingBuilder.setInitialRetryDelay(
          this.clientProperties.getSearchInitialRetryDelay());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "SearchInitialRetryDelay set to " + this.clientProperties.getSearchInitialRetryDelay());
      }
    }
    if (this.clientProperties.getSearchRetryDelayMultiplier() != null) {
      searchRetrySettingBuilder.setRetryDelayMultiplier(
          this.clientProperties.getSearchRetryDelayMultiplier());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "SearchRetryDelayMultiplier set to "
                + this.clientProperties.getSearchRetryDelayMultiplier());
      }
    }
    if (this.clientProperties.getSearchMaxRetryDelay() != null) {
      searchRetrySettingBuilder.setMaxRetryDelay(this.clientProperties.getSearchMaxRetryDelay());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "SearchMaxRetryDelay set to " + this.clientProperties.getSearchMaxRetryDelay());
      }
    }
    if (this.clientProperties.getSearchInitialRpcTimeout() != null) {
      searchRetrySettingBuilder.setInitialRpcTimeout(
          this.clientProperties.getSearchInitialRpcTimeout());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "SearchInitialRpcTimeout set to " + this.clientProperties.getSearchInitialRpcTimeout());
      }
    }
    if (this.clientProperties.getSearchRpcTimeoutMultiplier() != null) {
      searchRetrySettingBuilder.setRpcTimeoutMultiplier(
          this.clientProperties.getSearchRpcTimeoutMultiplier());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "SearchRpcTimeoutMultiplier set to "
                + this.clientProperties.getSearchRpcTimeoutMultiplier());
      }
    }
    if (this.clientProperties.getSearchMaxRpcTimeout() != null) {
      searchRetrySettingBuilder.setMaxRpcTimeout(this.clientProperties.getSearchMaxRpcTimeout());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "SearchMaxRpcTimeout set to " + this.clientProperties.getSearchMaxRpcTimeout());
      }
    }
    if (this.clientProperties.getSearchTotalTimeout() != null) {
      searchRetrySettingBuilder.setTotalTimeout(this.clientProperties.getSearchTotalTimeout());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("SearchTotalTimeout set to " + this.clientProperties.getSearchTotalTimeout());
      }
    }
    clientSettingsBuilder.searchSettings().setRetrySettings(searchRetrySettingBuilder.build());
    return SearchServiceClient.create(clientSettingsBuilder.build());
  }

  private HeaderProvider userAgentHeaderProvider() {
    String springLibrary = "spring-autogen-search-service";
    String version = this.getClass().getPackage().getImplementationVersion();
    return () -> Collections.singletonMap("user-agent", springLibrary + "/" + version);
  }
}
