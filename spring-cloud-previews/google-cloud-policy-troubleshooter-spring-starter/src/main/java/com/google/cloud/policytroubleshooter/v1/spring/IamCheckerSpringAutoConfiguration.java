/*
 * Copyright 2022 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

package com.google.cloud.policytroubleshooter.v1.spring;

import com.google.api.core.BetaApi;
import com.google.api.gax.core.CredentialsProvider;
import com.google.api.gax.core.ExecutorProvider;
import com.google.api.gax.httpjson.InstantiatingHttpJsonChannelProvider;
import com.google.api.gax.retrying.RetrySettings;
import com.google.api.gax.rpc.HeaderProvider;
import com.google.api.gax.rpc.TransportChannelProvider;
import com.google.cloud.policytroubleshooter.v1.IamCheckerClient;
import com.google.cloud.policytroubleshooter.v1.IamCheckerSettings;
import com.google.cloud.spring.core.Credentials;
import com.google.cloud.spring.core.DefaultCredentialsProvider;
import com.google.cloud.spring.global.GlobalProperties;
import java.io.IOException;
import java.util.Collections;
import javax.annotation.Generated;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.boot.autoconfigure.AutoConfiguration;
import org.springframework.boot.autoconfigure.condition.ConditionalOnClass;
import org.springframework.boot.autoconfigure.condition.ConditionalOnMissingBean;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.context.annotation.Bean;
import org.threeten.bp.Duration;

// AUTO-GENERATED DOCUMENTATION AND CLASS.
/**
 * Auto-configuration for {@link IamCheckerClient}.
 *
 * <p>Provides auto-configuration for Spring Boot
 *
 * <p>The default instance has everything set to sensible defaults:
 *
 * <ul>
 *   <li>The default transport provider is used.
 *   <li>Credentials are acquired automatically through Application Default Credentials.
 *   <li>Retries are configured for idempotent methods but not for non-idempotent methods.
 * </ul>
 */
@Generated("by google-cloud-spring-generator")
@BetaApi("Autogenerated Spring autoconfiguration is not yet stable")
@AutoConfiguration
@ConditionalOnClass(IamCheckerClient.class)
@ConditionalOnProperty(
    value = "com.google.cloud.policytroubleshooter.v1.spring.auto.iam-checker.enabled",
    matchIfMissing = true)
@EnableConfigurationProperties({IamCheckerSpringProperties.class, GlobalProperties.class})
public class IamCheckerSpringAutoConfiguration {
  private final IamCheckerSpringProperties clientProperties;
  private final GlobalProperties globalProperties;
  private static final Log LOGGER = LogFactory.getLog(IamCheckerSpringAutoConfiguration.class);

  protected IamCheckerSpringAutoConfiguration(
      IamCheckerSpringProperties clientProperties, GlobalProperties globalProperties) {
    this.clientProperties = clientProperties;
    this.globalProperties = globalProperties;
  }

  /**
   * Obtains the default credentials provider. The used key will be obtained from Spring Boot
   * configuration data files.
   */
  @Bean
  @ConditionalOnMissingBean
  public CredentialsProvider iamCheckerCredentials() throws IOException {
    if (this.clientProperties.getCredentials().hasKey()) {
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using credentials from IamChecker-specific configuration");
      }
      return ((CredentialsProvider) new DefaultCredentialsProvider(this.clientProperties));
    }
    if (LOGGER.isTraceEnabled()) {
      LOGGER.trace("Using credentials from global configuration");
    }
    return ((CredentialsProvider) new DefaultCredentialsProvider(this.globalProperties));
  }

  /**
   * Returns the default channel provider. The default is gRPC and will default to it unless the
   * useRest option is provided to use HTTP transport instead
   */
  @Bean
  @ConditionalOnMissingBean
  public TransportChannelProvider defaultIamCheckerTransportChannelProvider() {
    return IamCheckerSettings.defaultTransportChannelProvider();
  }

  /**
   * Provides a IamCheckerClient bean configured to use the default credentials provider (obtained
   * with iamCheckerCredentials()) and its default transport channel provider
   * (defaultIamCheckerTransportChannelProvider()). It also configures the quota project ID if
   * provided. It will configure an executor provider in case there is more than one thread
   * configured in the client
   *
   * <p>Individual retry settings are configured as well. It will use the relevant client library's
   * default retry settings when they are not specified in IamCheckerSpringProperties.
   */
  @Bean
  @ConditionalOnMissingBean
  public IamCheckerSettings iamCheckerSettings(
      @Qualifier("iamCheckerCredentials") CredentialsProvider credentialsProvider,
      @Qualifier("defaultIamCheckerTransportChannelProvider")
          TransportChannelProvider defaultTransportChannelProvider)
      throws IOException {
    IamCheckerSettings.Builder clientSettingsBuilder =
        IamCheckerSettings.newBuilder()
            .setCredentialsProvider(credentialsProvider)
            .setTransportChannelProvider(defaultTransportChannelProvider)
            .setHeaderProvider(this.userAgentHeaderProvider());
    if (this.clientProperties.getQuotaProjectId() != null) {
      clientSettingsBuilder.setQuotaProjectId(this.clientProperties.getQuotaProjectId());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Quota project id set to "
                + this.clientProperties.getQuotaProjectId()
                + ", this overrides project id from credentials.");
      }
    }
    if (this.clientProperties.getExecutorThreadCount() != null) {
      ExecutorProvider executorProvider =
          IamCheckerSettings.defaultExecutorProviderBuilder()
              .setExecutorThreadCount(this.clientProperties.getExecutorThreadCount())
              .build();
      clientSettingsBuilder.setBackgroundExecutorProvider(executorProvider);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Background executor thread count is "
                + this.clientProperties.getExecutorThreadCount());
      }
    }
    if (this.clientProperties.getUseRest()) {
      clientSettingsBuilder.setTransportChannelProvider(
          IamCheckerSettings.defaultHttpJsonTransportProviderBuilder().build());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using HTTP transport channel");
      }
    }
    RetrySettings.Builder troubleshootIamPolicyRetrySettingBuilder =
        clientSettingsBuilder.troubleshootIamPolicySettings().getRetrySettings().toBuilder();
    if (this.clientProperties.getTroubleshootIamPolicyInitialRpcTimeout() != null) {
      troubleshootIamPolicyRetrySettingBuilder.setInitialRpcTimeout(
          this.clientProperties.getTroubleshootIamPolicyInitialRpcTimeout());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "TroubleshootIamPolicyInitialRpcTimeout set to "
                + this.clientProperties.getTroubleshootIamPolicyInitialRpcTimeout());
      }
    }
    if (this.clientProperties.getTroubleshootIamPolicyRpcTimeoutMultiplier() != null) {
      troubleshootIamPolicyRetrySettingBuilder.setRpcTimeoutMultiplier(
          this.clientProperties.getTroubleshootIamPolicyRpcTimeoutMultiplier());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "TroubleshootIamPolicyRpcTimeoutMultiplier set to "
                + this.clientProperties.getTroubleshootIamPolicyRpcTimeoutMultiplier());
      }
    }
    if (this.clientProperties.getTroubleshootIamPolicyMaxRpcTimeout() != null) {
      troubleshootIamPolicyRetrySettingBuilder.setMaxRpcTimeout(
          this.clientProperties.getTroubleshootIamPolicyMaxRpcTimeout());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "TroubleshootIamPolicyMaxRpcTimeout set to "
                + this.clientProperties.getTroubleshootIamPolicyMaxRpcTimeout());
      }
    }
    if (this.clientProperties.getTroubleshootIamPolicyTotalTimeout() != null) {
      troubleshootIamPolicyRetrySettingBuilder.setTotalTimeout(
          this.clientProperties.getTroubleshootIamPolicyTotalTimeout());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "TroubleshootIamPolicyTotalTimeout set to "
                + this.clientProperties.getTroubleshootIamPolicyTotalTimeout());
      }
    }
    clientSettingsBuilder
        .troubleshootIamPolicySettings()
        .setRetrySettings(troubleshootIamPolicyRetrySettingBuilder.build());
    return clientSettingsBuilder.build();
  }

  @Bean
  @ConditionalOnMissingBean
  public IamCheckerClient iamCheckerClient(IamCheckerSettings iamCheckerSettings)
      throws IOException {
    return IamCheckerClient.create(iamCheckerSettings);
  }

  private HeaderProvider userAgentHeaderProvider() {
    String springLibrary = "spring-autogen-iam-checker";
    String version = this.getClass().getPackage().getImplementationVersion();
    return () -> Collections.singletonMap("user-agent", springLibrary + "/" + version);
  }
}
