/*
 * Copyright 2022 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

package com.google.cloud.dialogflow.v2.spring;

import com.google.api.core.BetaApi;
import com.google.api.gax.core.CredentialsProvider;
import com.google.api.gax.core.ExecutorProvider;
import com.google.api.gax.retrying.RetrySettings;
import com.google.api.gax.rpc.HeaderProvider;
import com.google.api.gax.rpc.TransportChannelProvider;
import com.google.cloud.dialogflow.v2.DocumentsClient;
import com.google.cloud.dialogflow.v2.DocumentsSettings;
import com.google.cloud.spring.core.DefaultCredentialsProvider;
import com.google.cloud.spring.core.Retry;
import com.google.cloud.spring.core.util.RetryUtil;
import java.io.IOException;
import java.util.Collections;
import javax.annotation.Generated;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.boot.autoconfigure.AutoConfiguration;
import org.springframework.boot.autoconfigure.condition.ConditionalOnClass;
import org.springframework.boot.autoconfigure.condition.ConditionalOnMissingBean;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.context.annotation.Bean;

// AUTO-GENERATED DOCUMENTATION AND CLASS.
/**
 * Auto-configuration for {@link DocumentsClient}.
 *
 * <p>Provides auto-configuration for Spring Boot
 *
 * <p>The default instance has everything set to sensible defaults:
 *
 * <ul>
 *   <li>The default transport provider is used.
 *   <li>Credentials are acquired automatically through Application Default Credentials.
 *   <li>Retries are configured for idempotent methods but not for non-idempotent methods.
 * </ul>
 */
@Generated("by google-cloud-spring-generator")
@BetaApi("Autogenerated Spring autoconfiguration is not yet stable")
@AutoConfiguration
@ConditionalOnClass(DocumentsClient.class)
@ConditionalOnProperty(
    value = "com.google.cloud.dialogflow.v2.spring.auto.documents.enabled",
    matchIfMissing = true)
@EnableConfigurationProperties(DocumentsSpringProperties.class)
public class DocumentsSpringAutoConfiguration {
  private final DocumentsSpringProperties clientProperties;
  private final CredentialsProvider credentialsProvider;
  private static final Log LOGGER = LogFactory.getLog(DocumentsSpringAutoConfiguration.class);

  protected DocumentsSpringAutoConfiguration(
      DocumentsSpringProperties clientProperties, CredentialsProvider credentialsProvider)
      throws IOException {
    this.clientProperties = clientProperties;
    if (this.clientProperties.getCredentials().hasKey()) {
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using credentials from Documents-specific configuration");
      }
      this.credentialsProvider =
          ((CredentialsProvider) new DefaultCredentialsProvider(this.clientProperties));
    } else {
      this.credentialsProvider = credentialsProvider;
    }
  }

  /**
   * Returns the default channel provider. The default is gRPC and will default to it unless the
   * useRest option is provided to use HTTP transport instead
   */
  @Bean
  @ConditionalOnMissingBean
  public TransportChannelProvider defaultDocumentsTransportChannelProvider() {
    return DocumentsSettings.defaultTransportChannelProvider();
  }

  /**
   * Provides a DocumentsClient bean configured to use the default credentials provider (obtained
   * with documentsCredentials()) and its default transport channel provider
   * (defaultDocumentsTransportChannelProvider()). It also configures the quota project ID if
   * provided. It will configure an executor provider in case there is more than one thread
   * configured in the client
   *
   * <p>Retry settings are also configured from service-level and method-level properties specified
   * in DocumentsSpringProperties. Method-level properties will take precedence over service-level
   * properties if available, and client library defaults will be used if neither are specified.
   */
  @Bean
  @ConditionalOnMissingBean
  public DocumentsSettings documentsSettings(
      @Qualifier("defaultDocumentsTransportChannelProvider")
          TransportChannelProvider defaultTransportChannelProvider)
      throws IOException {
    DocumentsSettings.Builder clientSettingsBuilder =
        DocumentsSettings.newBuilder()
            .setCredentialsProvider(this.credentialsProvider)
            .setTransportChannelProvider(defaultTransportChannelProvider)
            .setHeaderProvider(this.userAgentHeaderProvider());
    if (this.clientProperties.getQuotaProjectId() != null) {
      clientSettingsBuilder.setQuotaProjectId(this.clientProperties.getQuotaProjectId());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Quota project id set to "
                + this.clientProperties.getQuotaProjectId()
                + ", this overrides project id from credentials.");
      }
    }
    if (this.clientProperties.getExecutorThreadCount() != null) {
      ExecutorProvider executorProvider =
          DocumentsSettings.defaultExecutorProviderBuilder()
              .setExecutorThreadCount(this.clientProperties.getExecutorThreadCount())
              .build();
      clientSettingsBuilder.setBackgroundExecutorProvider(executorProvider);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Background executor thread count is "
                + this.clientProperties.getExecutorThreadCount());
      }
    }
    if (this.clientProperties.getUseRest()) {
      clientSettingsBuilder.setTransportChannelProvider(
          DocumentsSettings.defaultHttpJsonTransportProviderBuilder().build());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using HTTP transport channel");
      }
    }
    Retry serviceRetry = clientProperties.getRetry();
    if (serviceRetry != null) {
      RetrySettings listDocumentsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.listDocumentsSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.listDocumentsSettings().setRetrySettings(listDocumentsRetrySettings);

      RetrySettings getDocumentRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.getDocumentSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.getDocumentSettings().setRetrySettings(getDocumentRetrySettings);

      RetrySettings createDocumentRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.createDocumentSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.createDocumentSettings().setRetrySettings(createDocumentRetrySettings);

      RetrySettings importDocumentsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.importDocumentsSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder
          .importDocumentsSettings()
          .setRetrySettings(importDocumentsRetrySettings);

      RetrySettings deleteDocumentRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.deleteDocumentSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.deleteDocumentSettings().setRetrySettings(deleteDocumentRetrySettings);

      RetrySettings updateDocumentRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.updateDocumentSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.updateDocumentSettings().setRetrySettings(updateDocumentRetrySettings);

      RetrySettings reloadDocumentRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.reloadDocumentSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.reloadDocumentSettings().setRetrySettings(reloadDocumentRetrySettings);

      RetrySettings exportDocumentRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.exportDocumentSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.exportDocumentSettings().setRetrySettings(exportDocumentRetrySettings);

      RetrySettings listLocationsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.listLocationsSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.listLocationsSettings().setRetrySettings(listLocationsRetrySettings);

      RetrySettings getLocationRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.getLocationSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.getLocationSettings().setRetrySettings(getLocationRetrySettings);

      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured service-level retry settings from properties.");
      }
    }
    Retry listDocumentsRetry = clientProperties.getListDocumentsRetry();
    if (listDocumentsRetry != null) {
      RetrySettings listDocumentsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.listDocumentsSettings().getRetrySettings(), listDocumentsRetry);
      clientSettingsBuilder.listDocumentsSettings().setRetrySettings(listDocumentsRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for listDocuments from properties.");
      }
    }
    Retry getDocumentRetry = clientProperties.getGetDocumentRetry();
    if (getDocumentRetry != null) {
      RetrySettings getDocumentRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.getDocumentSettings().getRetrySettings(), getDocumentRetry);
      clientSettingsBuilder.getDocumentSettings().setRetrySettings(getDocumentRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for getDocument from properties.");
      }
    }
    Retry createDocumentRetry = clientProperties.getCreateDocumentRetry();
    if (createDocumentRetry != null) {
      RetrySettings createDocumentRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.createDocumentSettings().getRetrySettings(),
              createDocumentRetry);
      clientSettingsBuilder.createDocumentSettings().setRetrySettings(createDocumentRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for createDocument from properties.");
      }
    }
    Retry importDocumentsRetry = clientProperties.getImportDocumentsRetry();
    if (importDocumentsRetry != null) {
      RetrySettings importDocumentsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.importDocumentsSettings().getRetrySettings(),
              importDocumentsRetry);
      clientSettingsBuilder
          .importDocumentsSettings()
          .setRetrySettings(importDocumentsRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for importDocuments from properties.");
      }
    }
    Retry deleteDocumentRetry = clientProperties.getDeleteDocumentRetry();
    if (deleteDocumentRetry != null) {
      RetrySettings deleteDocumentRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.deleteDocumentSettings().getRetrySettings(),
              deleteDocumentRetry);
      clientSettingsBuilder.deleteDocumentSettings().setRetrySettings(deleteDocumentRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for deleteDocument from properties.");
      }
    }
    Retry updateDocumentRetry = clientProperties.getUpdateDocumentRetry();
    if (updateDocumentRetry != null) {
      RetrySettings updateDocumentRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.updateDocumentSettings().getRetrySettings(),
              updateDocumentRetry);
      clientSettingsBuilder.updateDocumentSettings().setRetrySettings(updateDocumentRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for updateDocument from properties.");
      }
    }
    Retry reloadDocumentRetry = clientProperties.getReloadDocumentRetry();
    if (reloadDocumentRetry != null) {
      RetrySettings reloadDocumentRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.reloadDocumentSettings().getRetrySettings(),
              reloadDocumentRetry);
      clientSettingsBuilder.reloadDocumentSettings().setRetrySettings(reloadDocumentRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for reloadDocument from properties.");
      }
    }
    Retry exportDocumentRetry = clientProperties.getExportDocumentRetry();
    if (exportDocumentRetry != null) {
      RetrySettings exportDocumentRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.exportDocumentSettings().getRetrySettings(),
              exportDocumentRetry);
      clientSettingsBuilder.exportDocumentSettings().setRetrySettings(exportDocumentRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for exportDocument from properties.");
      }
    }
    Retry listLocationsRetry = clientProperties.getListLocationsRetry();
    if (listLocationsRetry != null) {
      RetrySettings listLocationsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.listLocationsSettings().getRetrySettings(), listLocationsRetry);
      clientSettingsBuilder.listLocationsSettings().setRetrySettings(listLocationsRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for listLocations from properties.");
      }
    }
    Retry getLocationRetry = clientProperties.getGetLocationRetry();
    if (getLocationRetry != null) {
      RetrySettings getLocationRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.getLocationSettings().getRetrySettings(), getLocationRetry);
      clientSettingsBuilder.getLocationSettings().setRetrySettings(getLocationRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for getLocation from properties.");
      }
    }
    return clientSettingsBuilder.build();
  }

  @Bean
  @ConditionalOnMissingBean
  public DocumentsClient documentsClient(DocumentsSettings documentsSettings) throws IOException {
    return DocumentsClient.create(documentsSettings);
  }

  private HeaderProvider userAgentHeaderProvider() {
    String springLibrary = "spring-autogen-documents";
    String version = this.getClass().getPackage().getImplementationVersion();
    return () -> Collections.singletonMap("user-agent", springLibrary + "/" + version);
  }
}
