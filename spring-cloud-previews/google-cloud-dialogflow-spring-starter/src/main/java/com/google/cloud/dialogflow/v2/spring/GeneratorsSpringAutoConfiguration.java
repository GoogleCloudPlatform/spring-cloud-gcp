/*
 * Copyright 2024 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

package com.google.cloud.dialogflow.v2.spring;

import com.google.api.core.BetaApi;
import com.google.api.gax.core.CredentialsProvider;
import com.google.api.gax.core.ExecutorProvider;
import com.google.api.gax.retrying.RetrySettings;
import com.google.api.gax.rpc.HeaderProvider;
import com.google.api.gax.rpc.TransportChannelProvider;
import com.google.cloud.dialogflow.v2.GeneratorsClient;
import com.google.cloud.dialogflow.v2.GeneratorsSettings;
import com.google.cloud.spring.autoconfigure.core.GcpContextAutoConfiguration;
import com.google.cloud.spring.core.DefaultCredentialsProvider;
import com.google.cloud.spring.core.Retry;
import com.google.cloud.spring.core.util.RetryUtil;
import java.io.IOException;
import java.util.Collections;
import javax.annotation.Generated;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.boot.autoconfigure.AutoConfiguration;
import org.springframework.boot.autoconfigure.AutoConfigureAfter;
import org.springframework.boot.autoconfigure.condition.ConditionalOnClass;
import org.springframework.boot.autoconfigure.condition.ConditionalOnMissingBean;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.context.annotation.Bean;

// AUTO-GENERATED DOCUMENTATION AND CLASS.
/**
 * Auto-configuration for {@link GeneratorsClient}.
 *
 * <p>Provides auto-configuration for Spring Boot
 *
 * <p>The default instance has everything set to sensible defaults:
 *
 * <ul>
 *   <li>The default transport provider is used.
 *   <li>Credentials are acquired automatically through Application Default Credentials.
 *   <li>Retries are configured for idempotent methods but not for non-idempotent methods.
 * </ul>
 */
@Generated("by google-cloud-spring-generator")
@BetaApi("Autogenerated Spring autoconfiguration is not yet stable")
@AutoConfiguration
@AutoConfigureAfter(GcpContextAutoConfiguration.class)
@ConditionalOnClass(GeneratorsClient.class)
@ConditionalOnProperty(
    value = "com.google.cloud.dialogflow.v2.generators.enabled",
    matchIfMissing = true)
@EnableConfigurationProperties(GeneratorsSpringProperties.class)
public class GeneratorsSpringAutoConfiguration {
  private final GeneratorsSpringProperties clientProperties;
  private final CredentialsProvider credentialsProvider;
  private static final Log LOGGER = LogFactory.getLog(GeneratorsSpringAutoConfiguration.class);

  protected GeneratorsSpringAutoConfiguration(
      GeneratorsSpringProperties clientProperties, CredentialsProvider credentialsProvider)
      throws IOException {
    this.clientProperties = clientProperties;
    if (this.clientProperties.getCredentials().hasKey()) {
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using credentials from Generators-specific configuration");
      }
      this.credentialsProvider =
          ((CredentialsProvider) new DefaultCredentialsProvider(this.clientProperties));
    } else {
      this.credentialsProvider = credentialsProvider;
    }
  }

  /**
   * Provides a default transport channel provider bean, corresponding to the client library's
   * default transport channel provider. If the library supports both GRPC and REST transport, and
   * the useRest property is configured, the HTTP/JSON transport provider will be used instead of
   * GRPC.
   *
   * @return a default transport channel provider.
   */
  @Bean
  @ConditionalOnMissingBean(name = "defaultGeneratorsTransportChannelProvider")
  public TransportChannelProvider defaultGeneratorsTransportChannelProvider() {
    if (this.clientProperties.getUseRest()) {
      return GeneratorsSettings.defaultHttpJsonTransportProviderBuilder().build();
    }
    return GeneratorsSettings.defaultTransportChannelProvider();
  }

  /**
   * Provides a GeneratorsSettings bean configured to use a DefaultCredentialsProvider and the
   * client library's default transport channel provider
   * (defaultGeneratorsTransportChannelProvider()). It also configures the quota project ID and
   * executor thread count, if provided through properties.
   *
   * <p>Retry settings are also configured from service-level and method-level properties specified
   * in GeneratorsSpringProperties. Method-level properties will take precedence over service-level
   * properties if available, and client library defaults will be used if neither are specified.
   *
   * @param defaultTransportChannelProvider TransportChannelProvider to use in the settings.
   * @return a {@link GeneratorsSettings} bean configured with {@link TransportChannelProvider}
   *     bean.
   */
  @Bean
  @ConditionalOnMissingBean
  public GeneratorsSettings generatorsSettings(
      @Qualifier("defaultGeneratorsTransportChannelProvider")
          TransportChannelProvider defaultTransportChannelProvider)
      throws IOException {
    GeneratorsSettings.Builder clientSettingsBuilder;
    if (this.clientProperties.getUseRest()) {
      clientSettingsBuilder = GeneratorsSettings.newHttpJsonBuilder();
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using REST (HTTP/JSON) transport.");
      }
    } else {
      clientSettingsBuilder = GeneratorsSettings.newBuilder();
    }
    clientSettingsBuilder
        .setCredentialsProvider(this.credentialsProvider)
        .setTransportChannelProvider(defaultTransportChannelProvider)
        .setEndpoint(GeneratorsSettings.getDefaultEndpoint())
        .setHeaderProvider(this.userAgentHeaderProvider());
    if (this.clientProperties.getQuotaProjectId() != null) {
      clientSettingsBuilder.setQuotaProjectId(this.clientProperties.getQuotaProjectId());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Quota project id set to "
                + this.clientProperties.getQuotaProjectId()
                + ", this overrides project id from credentials.");
      }
    }
    if (this.clientProperties.getExecutorThreadCount() != null) {
      ExecutorProvider executorProvider =
          GeneratorsSettings.defaultExecutorProviderBuilder()
              .setExecutorThreadCount(this.clientProperties.getExecutorThreadCount())
              .build();
      clientSettingsBuilder.setBackgroundExecutorProvider(executorProvider);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Background executor thread count is "
                + this.clientProperties.getExecutorThreadCount());
      }
    }
    Retry serviceRetry = clientProperties.getRetry();
    if (serviceRetry != null) {
      RetrySettings createGeneratorRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.createGeneratorSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder
          .createGeneratorSettings()
          .setRetrySettings(createGeneratorRetrySettings);

      RetrySettings getGeneratorRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.getGeneratorSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.getGeneratorSettings().setRetrySettings(getGeneratorRetrySettings);

      RetrySettings listGeneratorsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.listGeneratorsSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.listGeneratorsSettings().setRetrySettings(listGeneratorsRetrySettings);

      RetrySettings deleteGeneratorRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.deleteGeneratorSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder
          .deleteGeneratorSettings()
          .setRetrySettings(deleteGeneratorRetrySettings);

      RetrySettings updateGeneratorRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.updateGeneratorSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder
          .updateGeneratorSettings()
          .setRetrySettings(updateGeneratorRetrySettings);

      RetrySettings listLocationsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.listLocationsSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.listLocationsSettings().setRetrySettings(listLocationsRetrySettings);

      RetrySettings getLocationRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.getLocationSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.getLocationSettings().setRetrySettings(getLocationRetrySettings);

      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured service-level retry settings from properties.");
      }
    }
    Retry createGeneratorRetry = clientProperties.getCreateGeneratorRetry();
    if (createGeneratorRetry != null) {
      RetrySettings createGeneratorRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.createGeneratorSettings().getRetrySettings(),
              createGeneratorRetry);
      clientSettingsBuilder
          .createGeneratorSettings()
          .setRetrySettings(createGeneratorRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for createGenerator from properties.");
      }
    }
    Retry getGeneratorRetry = clientProperties.getGetGeneratorRetry();
    if (getGeneratorRetry != null) {
      RetrySettings getGeneratorRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.getGeneratorSettings().getRetrySettings(), getGeneratorRetry);
      clientSettingsBuilder.getGeneratorSettings().setRetrySettings(getGeneratorRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for getGenerator from properties.");
      }
    }
    Retry listGeneratorsRetry = clientProperties.getListGeneratorsRetry();
    if (listGeneratorsRetry != null) {
      RetrySettings listGeneratorsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.listGeneratorsSettings().getRetrySettings(),
              listGeneratorsRetry);
      clientSettingsBuilder.listGeneratorsSettings().setRetrySettings(listGeneratorsRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for listGenerators from properties.");
      }
    }
    Retry deleteGeneratorRetry = clientProperties.getDeleteGeneratorRetry();
    if (deleteGeneratorRetry != null) {
      RetrySettings deleteGeneratorRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.deleteGeneratorSettings().getRetrySettings(),
              deleteGeneratorRetry);
      clientSettingsBuilder
          .deleteGeneratorSettings()
          .setRetrySettings(deleteGeneratorRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for deleteGenerator from properties.");
      }
    }
    Retry updateGeneratorRetry = clientProperties.getUpdateGeneratorRetry();
    if (updateGeneratorRetry != null) {
      RetrySettings updateGeneratorRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.updateGeneratorSettings().getRetrySettings(),
              updateGeneratorRetry);
      clientSettingsBuilder
          .updateGeneratorSettings()
          .setRetrySettings(updateGeneratorRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for updateGenerator from properties.");
      }
    }
    Retry listLocationsRetry = clientProperties.getListLocationsRetry();
    if (listLocationsRetry != null) {
      RetrySettings listLocationsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.listLocationsSettings().getRetrySettings(), listLocationsRetry);
      clientSettingsBuilder.listLocationsSettings().setRetrySettings(listLocationsRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for listLocations from properties.");
      }
    }
    Retry getLocationRetry = clientProperties.getGetLocationRetry();
    if (getLocationRetry != null) {
      RetrySettings getLocationRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.getLocationSettings().getRetrySettings(), getLocationRetry);
      clientSettingsBuilder.getLocationSettings().setRetrySettings(getLocationRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for getLocation from properties.");
      }
    }
    return clientSettingsBuilder.build();
  }

  /**
   * Provides a GeneratorsClient bean configured with GeneratorsSettings.
   *
   * @param generatorsSettings settings to configure an instance of client bean.
   * @return a {@link GeneratorsClient} bean configured with {@link GeneratorsSettings}
   */
  @Bean
  @ConditionalOnMissingBean
  public GeneratorsClient generatorsClient(GeneratorsSettings generatorsSettings)
      throws IOException {
    return GeneratorsClient.create(generatorsSettings);
  }

  private HeaderProvider userAgentHeaderProvider() {
    String springLibrary = "spring-autogen-generators";
    String version = this.getClass().getPackage().getImplementationVersion();
    return () -> Collections.singletonMap("user-agent", springLibrary + "/" + version);
  }
}
