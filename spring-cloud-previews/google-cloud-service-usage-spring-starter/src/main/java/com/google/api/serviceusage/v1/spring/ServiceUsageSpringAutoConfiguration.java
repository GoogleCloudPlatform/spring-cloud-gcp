/*
 * Copyright 2022 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

package com.google.api.serviceusage.v1.spring;

import com.google.api.core.BetaApi;
import com.google.api.gax.core.CredentialsProvider;
import com.google.api.gax.core.ExecutorProvider;
import com.google.api.gax.retrying.RetrySettings;
import com.google.api.gax.rpc.HeaderProvider;
import com.google.api.gax.rpc.TransportChannelProvider;
import com.google.api.serviceusage.v1.ServiceUsageClient;
import com.google.api.serviceusage.v1.ServiceUsageSettings;
import com.google.cloud.spring.core.DefaultCredentialsProvider;
import com.google.cloud.spring.core.Retry;
import com.google.cloud.spring.core.util.RetryUtil;
import java.io.IOException;
import java.util.Collections;
import javax.annotation.Generated;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.boot.autoconfigure.AutoConfiguration;
import org.springframework.boot.autoconfigure.condition.ConditionalOnClass;
import org.springframework.boot.autoconfigure.condition.ConditionalOnMissingBean;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.context.annotation.Bean;

// AUTO-GENERATED DOCUMENTATION AND CLASS.
/**
 * Auto-configuration for {@link ServiceUsageClient}.
 *
 * <p>Provides auto-configuration for Spring Boot
 *
 * <p>The default instance has everything set to sensible defaults:
 *
 * <ul>
 *   <li>The default transport provider is used.
 *   <li>Credentials are acquired automatically through Application Default Credentials.
 *   <li>Retries are configured for idempotent methods but not for non-idempotent methods.
 * </ul>
 */
@Generated("by google-cloud-spring-generator")
@BetaApi("Autogenerated Spring autoconfiguration is not yet stable")
@AutoConfiguration
@ConditionalOnClass(ServiceUsageClient.class)
@ConditionalOnProperty(
    value = "com.google.api.serviceusage.v1.spring.auto.service-usage.enabled",
    matchIfMissing = true)
@EnableConfigurationProperties(ServiceUsageSpringProperties.class)
public class ServiceUsageSpringAutoConfiguration {
  private final ServiceUsageSpringProperties clientProperties;
  private final CredentialsProvider credentialsProvider;
  private static final Log LOGGER = LogFactory.getLog(ServiceUsageSpringAutoConfiguration.class);

  protected ServiceUsageSpringAutoConfiguration(
      ServiceUsageSpringProperties clientProperties, CredentialsProvider credentialsProvider)
      throws IOException {
    this.clientProperties = clientProperties;
    if (this.clientProperties.getCredentials().hasKey()) {
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using credentials from ServiceUsage-specific configuration");
      }
      this.credentialsProvider =
          ((CredentialsProvider) new DefaultCredentialsProvider(this.clientProperties));
    } else {
      this.credentialsProvider = credentialsProvider;
    }
  }

  /**
   * Returns the default channel provider. The default is gRPC and will default to it unless the
   * useRest option is provided to use HTTP transport instead
   */
  @Bean
  @ConditionalOnMissingBean
  public TransportChannelProvider defaultServiceUsageTransportChannelProvider() {
    return ServiceUsageSettings.defaultTransportChannelProvider();
  }

  /**
   * Provides a ServiceUsageClient bean configured to use the default credentials provider (obtained
   * with serviceUsageCredentials()) and its default transport channel provider
   * (defaultServiceUsageTransportChannelProvider()). It also configures the quota project ID if
   * provided. It will configure an executor provider in case there is more than one thread
   * configured in the client
   *
   * <p>Retry settings are also configured from service-level and method-level properties specified
   * in ServiceUsageSpringProperties. Method-level properties will take precedence over
   * service-level properties if available, and client library defaults will be used if neither are
   * specified.
   */
  @Bean
  @ConditionalOnMissingBean
  public ServiceUsageSettings serviceUsageSettings(
      @Qualifier("defaultServiceUsageTransportChannelProvider")
          TransportChannelProvider defaultTransportChannelProvider)
      throws IOException {
    ServiceUsageSettings.Builder clientSettingsBuilder =
        ServiceUsageSettings.newBuilder()
            .setCredentialsProvider(this.credentialsProvider)
            .setTransportChannelProvider(defaultTransportChannelProvider)
            .setHeaderProvider(this.userAgentHeaderProvider());
    if (this.clientProperties.getQuotaProjectId() != null) {
      clientSettingsBuilder.setQuotaProjectId(this.clientProperties.getQuotaProjectId());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Quota project id set to "
                + this.clientProperties.getQuotaProjectId()
                + ", this overrides project id from credentials.");
      }
    }
    if (this.clientProperties.getExecutorThreadCount() != null) {
      ExecutorProvider executorProvider =
          ServiceUsageSettings.defaultExecutorProviderBuilder()
              .setExecutorThreadCount(this.clientProperties.getExecutorThreadCount())
              .build();
      clientSettingsBuilder.setBackgroundExecutorProvider(executorProvider);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Background executor thread count is "
                + this.clientProperties.getExecutorThreadCount());
      }
    }
    if (this.clientProperties.getUseRest()) {
      clientSettingsBuilder.setTransportChannelProvider(
          ServiceUsageSettings.defaultHttpJsonTransportProviderBuilder().build());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using HTTP transport channel");
      }
    }
    Retry serviceRetry = clientProperties.getRetry();
    if (serviceRetry != null) {
      RetrySettings enableServiceRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.enableServiceSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.enableServiceSettings().setRetrySettings(enableServiceRetrySettings);

      RetrySettings disableServiceRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.disableServiceSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.disableServiceSettings().setRetrySettings(disableServiceRetrySettings);

      RetrySettings getServiceRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.getServiceSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.getServiceSettings().setRetrySettings(getServiceRetrySettings);

      RetrySettings listServicesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.listServicesSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.listServicesSettings().setRetrySettings(listServicesRetrySettings);

      RetrySettings batchEnableServicesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.batchEnableServicesSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder
          .batchEnableServicesSettings()
          .setRetrySettings(batchEnableServicesRetrySettings);

      RetrySettings batchGetServicesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.batchGetServicesSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder
          .batchGetServicesSettings()
          .setRetrySettings(batchGetServicesRetrySettings);

      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured service-level retry settings from properties.");
      }
    }
    Retry enableServiceRetry = clientProperties.getEnableServiceRetry();
    if (enableServiceRetry != null) {
      RetrySettings enableServiceRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.enableServiceSettings().getRetrySettings(), enableServiceRetry);
      clientSettingsBuilder.enableServiceSettings().setRetrySettings(enableServiceRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for enableService from properties.");
      }
    }
    Retry disableServiceRetry = clientProperties.getDisableServiceRetry();
    if (disableServiceRetry != null) {
      RetrySettings disableServiceRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.disableServiceSettings().getRetrySettings(),
              disableServiceRetry);
      clientSettingsBuilder.disableServiceSettings().setRetrySettings(disableServiceRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for disableService from properties.");
      }
    }
    Retry getServiceRetry = clientProperties.getGetServiceRetry();
    if (getServiceRetry != null) {
      RetrySettings getServiceRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.getServiceSettings().getRetrySettings(), getServiceRetry);
      clientSettingsBuilder.getServiceSettings().setRetrySettings(getServiceRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for getService from properties.");
      }
    }
    Retry listServicesRetry = clientProperties.getListServicesRetry();
    if (listServicesRetry != null) {
      RetrySettings listServicesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.listServicesSettings().getRetrySettings(), listServicesRetry);
      clientSettingsBuilder.listServicesSettings().setRetrySettings(listServicesRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for listServices from properties.");
      }
    }
    Retry batchEnableServicesRetry = clientProperties.getBatchEnableServicesRetry();
    if (batchEnableServicesRetry != null) {
      RetrySettings batchEnableServicesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.batchEnableServicesSettings().getRetrySettings(),
              batchEnableServicesRetry);
      clientSettingsBuilder
          .batchEnableServicesSettings()
          .setRetrySettings(batchEnableServicesRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Configured method-level retry settings for batchEnableServices from properties.");
      }
    }
    Retry batchGetServicesRetry = clientProperties.getBatchGetServicesRetry();
    if (batchGetServicesRetry != null) {
      RetrySettings batchGetServicesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.batchGetServicesSettings().getRetrySettings(),
              batchGetServicesRetry);
      clientSettingsBuilder
          .batchGetServicesSettings()
          .setRetrySettings(batchGetServicesRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Configured method-level retry settings for batchGetServices from properties.");
      }
    }
    return clientSettingsBuilder.build();
  }

  @Bean
  @ConditionalOnMissingBean
  public ServiceUsageClient serviceUsageClient(ServiceUsageSettings serviceUsageSettings)
      throws IOException {
    return ServiceUsageClient.create(serviceUsageSettings);
  }

  private HeaderProvider userAgentHeaderProvider() {
    String springLibrary = "spring-autogen-service-usage";
    String version = this.getClass().getPackage().getImplementationVersion();
    return () -> Collections.singletonMap("user-agent", springLibrary + "/" + version);
  }
}
