/*
 * Copyright 2022 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

package com.google.cloud.retail.v2.spring;

import com.google.api.core.BetaApi;
import com.google.api.gax.core.CredentialsProvider;
import com.google.api.gax.core.ExecutorProvider;
import com.google.api.gax.retrying.RetrySettings;
import com.google.api.gax.rpc.HeaderProvider;
import com.google.api.gax.rpc.TransportChannelProvider;
import com.google.cloud.retail.v2.ProductServiceClient;
import com.google.cloud.retail.v2.ProductServiceSettings;
import com.google.cloud.spring.core.DefaultCredentialsProvider;
import com.google.cloud.spring.core.Retry;
import com.google.cloud.spring.core.util.RetryUtil;
import java.io.IOException;
import java.util.Collections;
import javax.annotation.Generated;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.boot.autoconfigure.AutoConfiguration;
import org.springframework.boot.autoconfigure.condition.ConditionalOnClass;
import org.springframework.boot.autoconfigure.condition.ConditionalOnMissingBean;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.context.annotation.Bean;

// AUTO-GENERATED DOCUMENTATION AND CLASS.
/**
 * Auto-configuration for {@link ProductServiceClient}.
 *
 * <p>Provides auto-configuration for Spring Boot
 *
 * <p>The default instance has everything set to sensible defaults:
 *
 * <ul>
 *   <li>The default transport provider is used.
 *   <li>Credentials are acquired automatically through Application Default Credentials.
 *   <li>Retries are configured for idempotent methods but not for non-idempotent methods.
 * </ul>
 */
@Generated("by google-cloud-spring-generator")
@BetaApi("Autogenerated Spring autoconfiguration is not yet stable")
@AutoConfiguration
@ConditionalOnClass(ProductServiceClient.class)
@ConditionalOnProperty(
    value = "com.google.cloud.retail.v2.spring.auto.product-service.enabled",
    matchIfMissing = true)
@EnableConfigurationProperties(ProductServiceSpringProperties.class)
public class ProductServiceSpringAutoConfiguration {
  private final ProductServiceSpringProperties clientProperties;
  private final CredentialsProvider credentialsProvider;
  private static final Log LOGGER = LogFactory.getLog(ProductServiceSpringAutoConfiguration.class);

  protected ProductServiceSpringAutoConfiguration(
      ProductServiceSpringProperties clientProperties, CredentialsProvider credentialsProvider)
      throws IOException {
    this.clientProperties = clientProperties;
    if (this.clientProperties.getCredentials().hasKey()) {
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using credentials from ProductService-specific configuration");
      }
      this.credentialsProvider =
          ((CredentialsProvider) new DefaultCredentialsProvider(this.clientProperties));
    } else {
      this.credentialsProvider = credentialsProvider;
    }
  }

  /**
   * Returns the default channel provider. The default is gRPC and will default to it unless the
   * useRest option is provided to use HTTP transport instead
   */
  @Bean
  @ConditionalOnMissingBean
  public TransportChannelProvider defaultProductServiceTransportChannelProvider() {
    return ProductServiceSettings.defaultTransportChannelProvider();
  }

  /**
   * Provides a ProductServiceClient bean configured to use the default credentials provider
   * (obtained with productServiceCredentials()) and its default transport channel provider
   * (defaultProductServiceTransportChannelProvider()). It also configures the quota project ID if
   * provided. It will configure an executor provider in case there is more than one thread
   * configured in the client
   *
   * <p>Retry settings are also configured from service-level and method-level properties specified
   * in ProductServiceSpringProperties. Method-level properties will take precedence over
   * service-level properties if available, and client library defaults will be used if neither are
   * specified.
   */
  @Bean
  @ConditionalOnMissingBean
  public ProductServiceSettings productServiceSettings(
      @Qualifier("defaultProductServiceTransportChannelProvider")
          TransportChannelProvider defaultTransportChannelProvider)
      throws IOException {
    ProductServiceSettings.Builder clientSettingsBuilder =
        ProductServiceSettings.newBuilder()
            .setCredentialsProvider(this.credentialsProvider)
            .setTransportChannelProvider(defaultTransportChannelProvider)
            .setHeaderProvider(this.userAgentHeaderProvider());
    if (this.clientProperties.getQuotaProjectId() != null) {
      clientSettingsBuilder.setQuotaProjectId(this.clientProperties.getQuotaProjectId());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Quota project id set to "
                + this.clientProperties.getQuotaProjectId()
                + ", this overrides project id from credentials.");
      }
    }
    if (this.clientProperties.getExecutorThreadCount() != null) {
      ExecutorProvider executorProvider =
          ProductServiceSettings.defaultExecutorProviderBuilder()
              .setExecutorThreadCount(this.clientProperties.getExecutorThreadCount())
              .build();
      clientSettingsBuilder.setBackgroundExecutorProvider(executorProvider);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Background executor thread count is "
                + this.clientProperties.getExecutorThreadCount());
      }
    }
    if (this.clientProperties.getUseRest()) {
      clientSettingsBuilder.setTransportChannelProvider(
          ProductServiceSettings.defaultHttpJsonTransportProviderBuilder().build());
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Using HTTP transport channel");
      }
    }
    Retry serviceRetry = clientProperties.getRetry();
    if (serviceRetry != null) {
      RetrySettings createProductRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.createProductSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.createProductSettings().setRetrySettings(createProductRetrySettings);

      RetrySettings getProductRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.getProductSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.getProductSettings().setRetrySettings(getProductRetrySettings);

      RetrySettings listProductsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.listProductsSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.listProductsSettings().setRetrySettings(listProductsRetrySettings);

      RetrySettings updateProductRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.updateProductSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.updateProductSettings().setRetrySettings(updateProductRetrySettings);

      RetrySettings deleteProductRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.deleteProductSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.deleteProductSettings().setRetrySettings(deleteProductRetrySettings);

      RetrySettings importProductsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.importProductsSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.importProductsSettings().setRetrySettings(importProductsRetrySettings);

      RetrySettings setInventoryRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.setInventorySettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder.setInventorySettings().setRetrySettings(setInventoryRetrySettings);

      RetrySettings addFulfillmentPlacesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.addFulfillmentPlacesSettings().getRetrySettings(),
              serviceRetry);
      clientSettingsBuilder
          .addFulfillmentPlacesSettings()
          .setRetrySettings(addFulfillmentPlacesRetrySettings);

      RetrySettings removeFulfillmentPlacesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.removeFulfillmentPlacesSettings().getRetrySettings(),
              serviceRetry);
      clientSettingsBuilder
          .removeFulfillmentPlacesSettings()
          .setRetrySettings(removeFulfillmentPlacesRetrySettings);

      RetrySettings addLocalInventoriesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.addLocalInventoriesSettings().getRetrySettings(), serviceRetry);
      clientSettingsBuilder
          .addLocalInventoriesSettings()
          .setRetrySettings(addLocalInventoriesRetrySettings);

      RetrySettings removeLocalInventoriesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.removeLocalInventoriesSettings().getRetrySettings(),
              serviceRetry);
      clientSettingsBuilder
          .removeLocalInventoriesSettings()
          .setRetrySettings(removeLocalInventoriesRetrySettings);

      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured service-level retry settings from properties.");
      }
    }
    Retry createProductRetry = clientProperties.getCreateProductRetry();
    if (createProductRetry != null) {
      RetrySettings createProductRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.createProductSettings().getRetrySettings(), createProductRetry);
      clientSettingsBuilder.createProductSettings().setRetrySettings(createProductRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for createProduct from properties.");
      }
    }
    Retry getProductRetry = clientProperties.getGetProductRetry();
    if (getProductRetry != null) {
      RetrySettings getProductRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.getProductSettings().getRetrySettings(), getProductRetry);
      clientSettingsBuilder.getProductSettings().setRetrySettings(getProductRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for getProduct from properties.");
      }
    }
    Retry listProductsRetry = clientProperties.getListProductsRetry();
    if (listProductsRetry != null) {
      RetrySettings listProductsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.listProductsSettings().getRetrySettings(), listProductsRetry);
      clientSettingsBuilder.listProductsSettings().setRetrySettings(listProductsRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for listProducts from properties.");
      }
    }
    Retry updateProductRetry = clientProperties.getUpdateProductRetry();
    if (updateProductRetry != null) {
      RetrySettings updateProductRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.updateProductSettings().getRetrySettings(), updateProductRetry);
      clientSettingsBuilder.updateProductSettings().setRetrySettings(updateProductRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for updateProduct from properties.");
      }
    }
    Retry deleteProductRetry = clientProperties.getDeleteProductRetry();
    if (deleteProductRetry != null) {
      RetrySettings deleteProductRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.deleteProductSettings().getRetrySettings(), deleteProductRetry);
      clientSettingsBuilder.deleteProductSettings().setRetrySettings(deleteProductRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for deleteProduct from properties.");
      }
    }
    Retry importProductsRetry = clientProperties.getImportProductsRetry();
    if (importProductsRetry != null) {
      RetrySettings importProductsRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.importProductsSettings().getRetrySettings(),
              importProductsRetry);
      clientSettingsBuilder.importProductsSettings().setRetrySettings(importProductsRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for importProducts from properties.");
      }
    }
    Retry setInventoryRetry = clientProperties.getSetInventoryRetry();
    if (setInventoryRetry != null) {
      RetrySettings setInventoryRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.setInventorySettings().getRetrySettings(), setInventoryRetry);
      clientSettingsBuilder.setInventorySettings().setRetrySettings(setInventoryRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace("Configured method-level retry settings for setInventory from properties.");
      }
    }
    Retry addFulfillmentPlacesRetry = clientProperties.getAddFulfillmentPlacesRetry();
    if (addFulfillmentPlacesRetry != null) {
      RetrySettings addFulfillmentPlacesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.addFulfillmentPlacesSettings().getRetrySettings(),
              addFulfillmentPlacesRetry);
      clientSettingsBuilder
          .addFulfillmentPlacesSettings()
          .setRetrySettings(addFulfillmentPlacesRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Configured method-level retry settings for addFulfillmentPlaces from properties.");
      }
    }
    Retry removeFulfillmentPlacesRetry = clientProperties.getRemoveFulfillmentPlacesRetry();
    if (removeFulfillmentPlacesRetry != null) {
      RetrySettings removeFulfillmentPlacesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.removeFulfillmentPlacesSettings().getRetrySettings(),
              removeFulfillmentPlacesRetry);
      clientSettingsBuilder
          .removeFulfillmentPlacesSettings()
          .setRetrySettings(removeFulfillmentPlacesRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Configured method-level retry settings for removeFulfillmentPlaces from properties.");
      }
    }
    Retry addLocalInventoriesRetry = clientProperties.getAddLocalInventoriesRetry();
    if (addLocalInventoriesRetry != null) {
      RetrySettings addLocalInventoriesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.addLocalInventoriesSettings().getRetrySettings(),
              addLocalInventoriesRetry);
      clientSettingsBuilder
          .addLocalInventoriesSettings()
          .setRetrySettings(addLocalInventoriesRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Configured method-level retry settings for addLocalInventories from properties.");
      }
    }
    Retry removeLocalInventoriesRetry = clientProperties.getRemoveLocalInventoriesRetry();
    if (removeLocalInventoriesRetry != null) {
      RetrySettings removeLocalInventoriesRetrySettings =
          RetryUtil.updateRetrySettings(
              clientSettingsBuilder.removeLocalInventoriesSettings().getRetrySettings(),
              removeLocalInventoriesRetry);
      clientSettingsBuilder
          .removeLocalInventoriesSettings()
          .setRetrySettings(removeLocalInventoriesRetrySettings);
      if (LOGGER.isTraceEnabled()) {
        LOGGER.trace(
            "Configured method-level retry settings for removeLocalInventories from properties.");
      }
    }
    return clientSettingsBuilder.build();
  }

  @Bean
  @ConditionalOnMissingBean
  public ProductServiceClient productServiceClient(ProductServiceSettings productServiceSettings)
      throws IOException {
    return ProductServiceClient.create(productServiceSettings);
  }

  private HeaderProvider userAgentHeaderProvider() {
    String springLibrary = "spring-autogen-product-service";
    String version = this.getClass().getPackage().getImplementationVersion();
    return () -> Collections.singletonMap("user-agent", springLibrary + "/" + version);
  }
}
